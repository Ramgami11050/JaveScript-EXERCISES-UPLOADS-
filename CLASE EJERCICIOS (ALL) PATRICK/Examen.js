
function initiate() {


    let wholenumber1 = parseInt(prompt("Enter the first number:"));
    let wholenumber2 = parseInt(prompt("Enter the second number:"));
    let wholenumber3 = parseInt(prompt("Enter the third number:"));

if(wholenumber3){
    if (wholenumber1 == wholenumber2 == wholenumber3) {
        alert("The numbers input are all different!");
    } else {
        alert("The numbers input are all the same!");
    };}

}

initiate()

/* Desarrolla un programa usando JavaScript y siendo ejecutado desde HTML en el navegador,
que permita a partir de tres números enteros indicar:
(2 puntos cada apartado)

Create a program using JS and being executed through HTML within the navegator
That allows from three whole numbers entered the following:

a. If they are all different
b. If they are all the same
c. The largest
d. The smallest
e. WIP

a. Si son todos diferentes.
b. Si son todos iguales.
c. El mayor.
d. El menor.
e. La media entre ellos (suma de todos, dividida entre la cantidad de números). */